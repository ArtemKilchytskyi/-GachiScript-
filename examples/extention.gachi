ass GachiPerson {
    fatCock(name, sm) {
        slave._name = name;
        slave._sm = sm;
    }

    suckSemen name () {
        fuckingCumming slave._name
    }
    
    suckSemen sm () {
        fuckingCumming slave._sm
    }

    getInfo() {
        fuckingCumming {name: slave._name, sm: slave._sm}
    }
}

vault.suck = {GachiPerson}
